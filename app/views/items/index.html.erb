<h1>Listing items</h1>


<table>
  <tr>
    <th>Rota</th>
    <th>Logradouro</th>
    <th>Endereco</th>
    <th>Bairro</th>
    <th>Cidade</th>
    <th>Estado</th>
    <th>Cep</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @items.each do |item| %>
  <tr>
    <td><%= item.route.nome %></td>
    <td><%= item.logradouro %></td>
    <td><%= item.endereco %></td>
    <td><%= item.bairro %></td>
    <td><%= item.cidade %></td>
    <td><%= item.estado %></td>
    <td><%= item.cep %></td>
    <td><%= link_to 'Show', item %></td>
    <td><%= link_to 'Edit', edit_item_path(item) %></td>
    <td><%= link_to 'Destroy', item, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Ver rotas', routes _path %>
<%= link_to 'Adicionar CEP', new_item_path %>
